#!/bin/bash
# One tick of the Slack gate. launchd calls this every 60 seconds.
# Exits immediately when no gate is open, so it costs nothing most of the week.
set -euo pipefail
cd "$(dirname "$0")/.."
exec /usr/bin/env python3 -m pipeline.gate_runner "$@"
