# Every credential file under config/, never committed.
# Broader than naming each one, so a new *.env cannot be added unprotected.
config/*.env
config/NEW_TOPIC.txt

# pull artefacts
data/notes_dump.md
data/notes_dump.json
data/lead_data_previous.json
data/note_summaries.json
data/gate_state.json

# rehearsal output, never part of the archive
briefs/test-*.html

# macOS
.DS_Store

# test scratch dirs
tmp*/
